services:
  - type: static
    name: healthcare-dashboard
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://healthcare-platform-api.onrender.com
